apiVersion: apps/v1
kind: Deployment
metadata:
  name: wisecow-deployment
  namespace: wisecow
  labels:
    app: wisecow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wisecow
  template:
    metadata:
      labels:
        app: wisecow
    spec:
      containers:
      - name: wisecow
        # replace <DOCKER_USERNAME> with your Docker Hub username
        image: katsura11/wisecow:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4499
        readinessProbe:
          exec:
            command:
              - nc
              - -z
              - localhost
              - "4499"
          initialDelaySeconds: 2
          periodSeconds: 5
